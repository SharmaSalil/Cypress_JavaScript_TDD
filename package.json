{
  "name": "cypress_dew_demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:directories": "rm -rf cypress/screenshots && mkdir -p cypress/screenshots && rm -rf cypress/reports && mkdir -p cypress/reports",
    "test-execution": "npm run clean:directories && npx cypress run --spec ./cypress/e2e/Registration/registration.cy.js, ./cypress/e2e/login/login.cy.js --headed"
  },
  "author": "",
  "authorLinkedinProfile": "",
  "authorGitProfile": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.10.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}